# Application shell and Firebase readiness

The static Istanbul client loads and reaches the local Firebase emulators.

## The bazaar is ready for its first merchants

![The bazaar is ready for its first merchants](./screenshots/000-firebase-ready-desktop.png)

**Verifications:**

- [x] The page exposes the stable Istanbul title
- [x] The landing page introduces the four-step route to a ruby
- [x] The client reaches the Firebase emulators through anonymous authentication
- [x] The deterministic short build marker is visible
